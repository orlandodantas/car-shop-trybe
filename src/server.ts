import App from './app';
// import handleError from './middleware/handleError';
// import CarRouter from './routes/carRouter';

const server = new App();

// server.addRouter('/cars', CarRouter);

// server.app.use(handleError);
export default server;
